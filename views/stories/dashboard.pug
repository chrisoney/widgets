extends ../layout

include ../utils

append head
  link(rel="stylesheet" href="/stylesheets/dashboard.css")
  //- add page specific js
  script(src="/javascripts/dashboard.js" type="module" defer)

block content
  div(class="main-container")
    h1 Stories dashboard
    div(class="dashboard-container")

      div(class="stories-container")
          ul
            each story, idx in stories
              - var recommendation = story.recommendation[0]
              li(class="story-container")
                div(class="story-header-container")
                  span(class="story-title")=story.title
                  div(class="story-button-container")
                    span(class=`fas fa-chevron-circle-left toggle-${idx}`)
                    span(class="fas fa-arrow-circle-right")
                div(class=`story-details-container container-${idx}`)
                  div(class="story-details-top")
                    div(class="story-details-top-left")
                      +dashStoryDetail("Book:", story.Subscription.book, story.id)
                      +dashStoryDetail("Chapter:", story.Subscription.chapter, story.id)
                      a(href=story.link class="link") Link
                    div(class="story-details-top-right")
                      div(class="stars")
                        - var i = 0, j = 5
                        while i < recommendation.rating
                          span(class="fas fa-star")
                          - i++
                        while j > recommendation.rating
                          span(class="far fa-star")
                          - j--

                  div(class="story-details-bottom")
                    div(class="description-container")
                      span(class="reveal") Description
                      p(class="description-text")= story.description
                    div(class="review-container")
                      span(class="reveal") Review
                      p(class="review-text")= recommendation.review
      include nav-section